---
import { Image } from "astro:assets";

interface Props {
	alt: string;
	class?: string | null;
	src: ImageMetadata | string;
}
const { src, alt, class: className } = Astro.props;
---

<Image alt={alt} class={`mx-auto ${className ?? ""}`} src={src} />
